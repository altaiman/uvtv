include data.pug

mixin sliderCourses(data)
  +slider({
    title: data.title,
    slider: 'items'
  })
    each item in coursesData
      +e.LI.slide
        +item_course({
          title: item.title,
          img: item.img
        })
